# Composable Safari SDK

Planned: TypeScript tab handles, snapshots, semantic locators, bounded batches and condition waits. A local object may survive across calls, but the bridge remains authoritative for session and document identity.

This component is not implemented. Do not claim API or behavioral compatibility with Playwright or any proprietary browser SDK. Implement an explicit subset with capability reporting and structured unsupported-operation errors. Share protocol types with the bridge and MCP adapter.
